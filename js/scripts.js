// Business Logic //

function mrRobo(number) {
  let inputArray = [];
  for (let i = 0; i <= number; i++) {
    inputArray.push(i.toString());
  }
  return inputArray
}

function roboBeep(array)
  let inputArray = mrRobo();
  let beepArray = []
  for (let i = 0; i <= inputArray.length; i++)
    if ([i])